{
  "name": "glslog",
  "version": "0.0.8",
  "license": "MIT",
  "author": "Juha Järvi",
  "main": "dist/index.js",
  "browser": "dist/browser.js",
  "scripts": {
    "prepare": "node src/patch.cjs && tsc -d --emitDeclarationOnly --outDir lib src/Swizzle.ts && node src/generate-libs.js && tsc -p src && vite build && mv dist/index.js dist/browser.js && rollup -c",
    "test": "node dist/test.js src/pen.vert",
    "start": "vite"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "rollup": "^4.22.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.6"
  }
}
